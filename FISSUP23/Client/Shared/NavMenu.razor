@namespace FISSUP23.Client.Shared

<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">MDeeBee</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" @onclick="() => expandOverfoMenu = !expandOverfoMenu" href="./overforingar">
                <span class="oi oi-home" aria-hidden="true"></span> Main
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="./overforingarView">
                <span class="oi oi-list-rich" aria-hidden="true"></span> Överforingar
            </NavLink>
            <NavLink class="nav-link" href="./filkollektioner">
                <span class="oi oi-list-rich" aria-hidden="true"></span> Filkollektioner
            </NavLink>
            <NavLink class="nav-link" href="./filer">
                <span class="oi oi-list-rich" aria-hidden="true"></span> Filer
            </NavLink>
            <NavLink class="nav-link" href="./kolumner">
                <span class="oi oi-list-rich" aria-hidden="true"></span> Kolumner
            </NavLink>
            <NavLink class="nav-link" href="./processdata">
                <span class="oi oi-list-rich" aria-hidden="true"></span> Processdata
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="contact">
                <span class="oi oi-envelope-closed" aria-hidden="true"></span> Kontakt
            </NavLink>
        </div>
    </nav>
</div>

@code {

    private bool expandOverfoMenu = false;
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }

}